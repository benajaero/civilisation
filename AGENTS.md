# Repository Rules

- Treat `packages/brand` as the canonical source of truth.
- Treat `apps/web` as a consumer of shared brand content and workspace packages.
- Keep governance, provenance, and editorial policies in `docs/policies`.
- Prefer workspace packages over duplicating shared content or rules in apps.
- Make changes through the documented review flow before publication.
