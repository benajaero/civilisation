# Contributing

This repository uses pull requests for all contributions.

- Use `docs/` for policy and process documentation.
- Use `packages/` for shared workspace content and code.
- Use `apps/` for application shells and consumers.
- Require review before merging any change that affects shared governance or published content.
